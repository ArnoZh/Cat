using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Cat.Scada.Equips.Crane
{
    /// <summary>
    /// 连线时的电控信息，不连线时的数据信息
    /// </summary>
    public class CraneInformation : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
